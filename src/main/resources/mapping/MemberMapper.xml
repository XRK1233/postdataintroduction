<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pos.postdataintroduction.mapper.MemberMapper">

    <resultMap id="BaseResultMap" type="com.pos.postdataintroduction.domain.Member">
        <result column="id" jdbcType="INTEGER" property="id" />
        <result column="userName" jdbcType="VARCHAR" property="userName" />
        <result column="passWord" jdbcType="VARCHAR" property="passWord" />
        <result column="realName" jdbcType="VARCHAR" property="realName" />
    </resultMap>

    <select id="Sel" resultType="com.pos.postdataintroduction.domain.Member">
        select * from user where id = #{id}
    </select>
    <!--插入一条数据-->
    <insert id="addMember" useGeneratedKeys="true" keyProperty="agentId"  parameterType="com.pos.postdataintroduction.domain.Member" >
      insert  into t_a_pos_memberdata (company_name,company_short_name,file_num,mchid) values (#{companyName},#{companyShortName},#{fileNmae},#{mchid})
    </insert>
</mapper>
